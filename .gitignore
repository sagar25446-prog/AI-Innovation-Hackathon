__pycache__/
*.py[cod]
.pytest_cache/
.env
.venv/
venv/
node_modules/
.DS_Store

# Manim writes its intermediate text/SVG cache next to the process cwd.
# Rendered lesson videos are cached outside the repo; only the curated
# demo-assets/videos/ are committed (see demo-assets/README.md).
media/
*.log

# facexlib downloads its weights relative to the working directory, so running
# SadTalker tooling from the repo root drops ~290MB here.
gfpgan/
*_sadtalker_crop.png
